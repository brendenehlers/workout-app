apiVersion: v1
kind: Service
metadata:
  name: webserver-service-dev
spec:
  selector:
    app: webserver-dev
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: http-web-svc
    protocol: TCP
